# Valdi Compact Compiler - VCC

This is the compiler project for the class **DIM0164 - Compilers**. 

## How to build

This is a C++20 project. To compile, run ```cmake -S . -B build```

## How to run

To run the file preprocessor for .java files, run ```vcc <input_file>.java -p <outputfile>.txt```